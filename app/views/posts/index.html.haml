-if params[:tag]
  %p.tag
    Filtrado por tag 
    %strong= params[:tag]
    = link_to "Regresar", post_path

#sec_1
  - @posts.each do|post|
   
    - stats = stats(post.id);
    %div.post{:id=>"post-"+post.id.to_s}
      = link_to (image_tag post.imagen(:grande)), post_path(post)
      %h1 
        %span
          Hot Now
        = " - " + post.title
      %p
        = post.extracto.html_safe
      %ul.tools
        %li.fecha
          %i
          = "Fecha: "+post.created_at.to_date.to_s
        %li.coments
          %i
          = "Comentarios: "+ stats.comments.count.to_s
        %li.cat
          %i
          - if stats.categorias.count > 0
            Categoria: HotNow
          - else
            Categoria: Sephora
        %li.loves
          %i
          Loves: 1
      %ul.tags
        %li.tag
          %i
          Tags:
        %li Uno
        %li Dos
        %li Tres
        %li Cuatro
        %li Cinco
      = link_to "leer más...", post_path(post), {:class=>"leer_mas"}
#sec_2
  .widget
    %h2 Ultimos Tweets
  .widget
    %h2 Facebook
  .widget
    %h2 Instagram
    - @recientes.each do |data|
      = image_tag(data.images.thumbnail.url, :width => 100, :height => 100, :class => "insta_pic")
  .widget
    %h2 Las 5 más vistas

