:css
  .rel{
    position: relative;
    padding-top: 530px !important;
  }
  .link_me{
    position: relative;
    height: 270px;
    width: 748px;
    //background: rgba(250, 0, 0, 0.6);
    display: block;
    margin: 10px auto;
    z-index: 2;
    cursor: pointer;
  }
  .float_me{
    position: absolute;
    top: 0;
    z-index: 1;
    left: -25%;
    margin-left: 345px;
  }
  .last-item{
    display: block;
    height: 1px;
    width: 100%;
    margin-top: 50px;
  }

%section.rel
  =image_tag("brow-collection-landing.jpg", {class: "float_me"})
  =link_to(producto_ver_path(Producto.find_by_upc("602004071613").slug)) do
    .link_me
  =link_to(producto_ver_path(Producto.find_by_upc("602004071675").slug)) do
    .link_me
  =link_to(producto_ver_path(Producto.find_by_upc("602004071590").slug)) do
    .link_me
  =link_to(producto_ver_path(Producto.find_by_upc("602004071316").slug)) do
    .link_me
  =link_to(producto_ver_path(Producto.find_by_upc("602004071293").slug)) do
    .link_me
  =link_to(producto_ver_path(Producto.find_by_upc("602004071248").slug)) do
    .link_me
  =link_to(producto_ver_path(Producto.find_by_upc("602004071705").slug)) do
    .link_me
  =link_to(producto_ver_path(Producto.find_by_upc("602004071378").slug)) do
    .link_me
  =link_to(producto_ver_path(Producto.find_by_upc("602004067456").slug)) do
    .link_me
  .last-item
  .clearfix
